% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cABC_plot_style.R
\name{cABC_plot_style}
\alias{cABC_plot_style}
\title{ABC Plot style}
\usage{
cABC_plot_style(
  LineWidth = NULL,
  ShowUniform = NULL,
  ShowBoundary = NULL,
  Plot_title = NULL,
  UniformColor = NULL,
  IdentityColor = NULL,
  EquilibriumColor = NULL,
  CurveColor = NULL,
  PointColor = NULL,
  ABoundaryColor = NULL,
  BBoundaryColor = NULL,
  CBoundaryColor = NULL,
  BoundaryLineColor = NULL,
  LabelColor = NULL,
  LegendTextSize = NULL,
  LegendX = NULL,
  LegendY = NULL,
  LegendSpacing = NULL,
  Theme = NULL
)
}
\arguments{
\item{LineWidth}{Numeric. Line width of the main ABC curve. Default
is \code{1.25}.}

\item{ShowUniform}{Logical. Whether to draw the uniform reference
curve. Default is \code{TRUE}.}

\item{ShowBoundary}{Logical. Whether to draw lines indicating
boundaries. Default is \code{TRUE}.}

\item{Plot_title}{Character. Title of the plot. Default is
\code{"ABC plot"}.}

\item{UniformColor}{Color of the uniform reference curve. Any valid R
color specification, e.g. a color name (\code{"green"}), a hex code
(\code{"#00FF00"}), or a palette index (\code{colors()[26]}).
Default is \code{"green"}.}

\item{IdentityColor}{Color of the identity line (y = x). Any valid R
color specification (see \code{UniformColor}). Default is
\code{grDevices::colors()[452]}.}

\item{EquilibriumColor}{Color of the equilibrium diagonal (y = 1 - x).
Any valid R color specification (see \code{UniformColor}). Default
is \code{grDevices::colors()[175]}.}

\item{CurveColor}{Color of the ABC curve itself. Any valid R color
specification (see \code{UniformColor}). Default is \code{"blue"}.}

\item{PointColor}{Color of individual data points (shown when n < 20).
Any valid R color specification (see \code{UniformColor}). Default
is \code{"blue"}.}

\item{ABoundaryColor}{Color of the A|B boundary star and label. Any
valid R color specification (see \code{UniformColor}). Default is
\code{"red"}.}

\item{BBoundaryColor}{Color of the B boundary star. Any valid R color
specification (see \code{UniformColor}). Default is \code{"green"}.}

\item{CBoundaryColor}{Color of the B|C boundary star and label. Any
valid R color specification (see \code{UniformColor}). Default is
\code{"blue"}.}

\item{BoundaryLineColor}{Color of the orthogonal boundary guide lines.
Any valid R color specification (see \code{UniformColor}). Default
is \code{"red"}.}

\item{LabelColor}{Color of the A/B/C set labels and counts. Any valid
R color specification (see \code{UniformColor}). Default is
\code{"red"}.}

\item{LegendTextSize}{Numeric. Font size of the legend text. Default
is \code{4}.}

\item{LegendX}{Numeric. X-position of the legend. Default is
\code{0.80}.}

\item{LegendY}{Numeric, or \code{NULL} (default). Y-position where
the legend starts. If \code{NULL}, this is calculated automatically
based on the A/C boundary positions to avoid overlapping the plot.}

\item{LegendSpacing}{Numeric. Vertical spacing between legend entries.
Default is \code{0.05}.}

\item{Theme}{A ggplot2 theme object (e.g. \code{ggplot2::theme_minimal()}).
Default is \code{ggplot2::theme_light()} with a few structural
tweaks applied on top (no panel grid, a bordered panel, and a
centered plot title).}
}
\value{
A named list of plot options, suitable for the \code{plot_args}
  argument of \code{\link{cABC_analysis}}.
}
\description{
Constructs a validated list of cosmetic options for customizing ABC
plots produced by \code{\link{cABC_analysis}}. Pass the result to
\code{cABC_analysis(..., plot_args = cABC_plot_style(...))}.
}
\details{
Any option left at \code{NULL} falls back to the package default 
\code{\link{cABC_default_plot_args}}.
}
\examples{
opts <- cABC_plot_style(LineWidth = 2, CurveColor = "darkred")
data("SwissInhabitants")
abc <- cABC_analysis(SwissInhabitants, PlotIt = TRUE, plotArgs = opts)

}
\seealso{
\code{\link{cABC_analysis}}
}
